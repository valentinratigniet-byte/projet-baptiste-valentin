name: 'dbt_cg'
version: '1.0.0'
config-version: 2

profile: 'dbt_cg'

model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  dbt_cg:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
